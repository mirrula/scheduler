<div class="modal fade" id="newlist" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Create new list</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= form_with model: @new_list do |f| %>
            <%= f.text_field :name, placeholder: "list name" %>
            <%= f.text_field :icon, placeholder: "Image url" %>
            <%= f.submit "Create list" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
